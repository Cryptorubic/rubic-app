export interface ExpirationValue {
  /**
   * Shortened version of full value, displayed in main block.
   */
  shortValue: string;

  /**
   * Full value, displayed in hint (if not equal to short value).
   */
  fullValue: string;
}
