import { EthAddressDirective } from './eth-address.directive';

describe('EthAddressDirective', () => {
  it('should create an instance', () => {
    const directive = new EthAddressDirective();

    expect(directive).toBeTruthy();
  });
});
