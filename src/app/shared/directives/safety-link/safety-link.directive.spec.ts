import { SafetyLinkDirective } from './safety-link.directive';

describe('SafetyLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new SafetyLinkDirective();
    expect(directive).toBeTruthy();
  });
});
