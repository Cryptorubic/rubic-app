import { TokenAddressDirective } from './token-address.directive';

describe('TokenAddressDirective', () => {
  it('should create an instance', () => {
    const directive = new TokenAddressDirective(null);
    expect(directive).toBeTruthy();
  });
});
