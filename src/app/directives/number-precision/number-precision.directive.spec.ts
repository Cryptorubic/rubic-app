import { NumberPrecisionDirective } from './number-precision.directive';

describe('NumberPrecisionDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberPrecisionDirective();

    expect(directive).toBeTruthy();
  });
});
