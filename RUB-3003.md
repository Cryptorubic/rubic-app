TODO:

- 'Connect Wallet & Send' always same text even if wallet is connected
- fix tradeId crosses formTitle on mobile
- ++ add mobile UI
- add success modal OR ask maybe add success info in 4 step when it's done, because statuses will be
  not visible otherwise
- remove SwapPrivateResponseInterface from monorepo
- ++ add data/to/value in swap response
- pass receiver and refund params in CrossChainService.swapTrade and OnChainService.swapTrade

PROBLEMS with task:

- switchChain / handle wallet change / handle different wallet chainType / handle swap errors

**DEPOSIT_FORM_STATES: DepositFormState**

- TRADE_EXPIRED
- IDLE
- WAITING_FOR_SENDING_DEPOSIT
- STATUS_TRACKING: DepositTradeStatus(awaiting, confirming, exchanging, sending_to_receiver,
  completed)
- COMPLETED

**DEPOSIT_TRADE_STEPS: DepositTradeStep**

- EXCHANGE_DETAILS
- INPUT_ADDRESSES
- TRADE_INFO
- TRADE_STATUS

`service class DepositFormManager`

- public closeForm(): void { this.depositStatusSrv.stopPolling() } - called in onDestroy hook
- private depositFormState: DepositFormState
- private _steps$ = nwe BehaviourSubject<Array<DepositStep | DepositStepWithBtn>>([])
- public steps$ = this._steps$.asObservable()
- private setLockedSteps(): void
- private setUnlockedSteps(): void
- public setDepositFormState(state: DepositFormState): void
- public doBtnAction(actionType: DepositTradeStep): void
- public getActionBtnText(): string
- public getFormTitle(): string

`service class DepositStatusService`

- private _pollingSub$: Subscription | null = null;
- private _status$ = new BehaviourSubject<DepositTradeStatus>('awaiting')
- public status$ = this._status$.asObservable()
- private async fetchStatus(rubicId: string): DepositTxStatus
- public stopPolling(): void { this._pollingSub$.unsubscribe() }
- public initPolling(): void { interval(30_000)... }

`abstract class DepositStep`

- active: boolean
- formTitle: string
- setActive(active: boolean): void
- setFormTitle(title: string): void

`abstract class DepositStepWithBtn extends DepositStep`

- confirmed: boolean
- setConfirmed(confirmed: boolean): void
- actionBtnText: string
- async doBtnAction(): void
- updateActionBtnText(text: string): void

**UI**

- for each accordion - different component DepositStepComponent
- for the whole form DepositForm with 4 steps
- also create wrapper for TRADE_EXPIRED and COMPLETED
- on InputAddressesDepositStep: new FormGroup({ receiverAddr: string, refundAddr: string })
